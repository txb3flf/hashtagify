{
  "name": "online-interview",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.5.0"
  },
  "scripts": {
    "test": "$(npm bin)/jest",
    "test.watch": "$(npm bin)/jest --watch"
  }
}
